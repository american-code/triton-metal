.DS_Store
.build/
__pycache__/
*.egg-info/
.venv/
build/
dist/
.pytest_cache/
*.metallib

# Staged into the plugin backend by Tools/bundle-wheel.sh so the Triton wheel
# carries the Swift runtime. Built, not source.
python/plugin/backend/libtritonmetal.dylib
